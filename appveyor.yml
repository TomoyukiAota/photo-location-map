environment:
  matrix:
    - nodejs_version: "10"      # Highest LTS version available in AppVeyor
    - nodejs_version: "11"      # Latest version available in AppVeyor

platform:
  - x64

# Install scripts. (runs after repo cloning)
install:
  - choco upgrade googlechrome
  - dir C:\avvm\node            # Print available versions of Node.js in AppVeyor
  - ps: Install-Product node $env:nodejs_version $env:platform
  - npm set progress=false
  - npm install

# Post-install test scripts.
test_script:
  # Output useful info for debugging.
  - node --version
  - npm --version
  # run tests
  - npm run lint:ng
  - npm run lint:es
  - npm run test:unit:karma
  - npm run test:unit:em:main
  # - npm run test:e2e
  - npm run test:package

# Don't actually build.
build: off
